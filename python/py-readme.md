# #Python

Python is useful, and many have used it. I am simply documenting some ways I use it and hopefully provide some tips/tricks. 

example: learning.py uses the ```from package_name import *``` and also shows how things like Flask are imported like ```import package_name as Package```

